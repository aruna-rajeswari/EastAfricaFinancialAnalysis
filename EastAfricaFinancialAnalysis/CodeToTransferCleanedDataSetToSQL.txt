from sqlalchemy import create_engine
import pandas as pd

# MySQL credentials
username = 'root'
password = 'root'
host = 'localhost'       
port = '3306'            # default MySQL port
database = 'EastAfricaFinData'

# Creating the SQLAlchemy engine
engine = create_engine(f'mysql+pymysql://{username}:{password}@{host}:{port}/{database}')

# Read CSV file
df = pd.read_csv('C:/Aruna/Fortrary/DA Workshop/4-Assignment Project/Cleaned_financial_inclusion_dataset.csv')

# To Import the data into MySQL table 'users'
df.to_sql('users', con=engine, if_exists='replace', index=False)

print("Data imported successfully!")